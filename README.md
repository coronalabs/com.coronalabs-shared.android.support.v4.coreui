Sources for the plugin `shared.android.support.v4.coreui`.

Add following to your `build.settings` to use:
```lua
{
    plugins = {
        "shared.android.support.v4.coreui" = {
            publisherId = "com.coronalabs",
        },
    },
}
```
